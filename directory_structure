root_directory/
│
├──app.py
├── utils.py
├── data/
├── models/
├── notebooks/
├── prompts/
├── Dockerfile
├── README.md
└── requirements.txt

docker tag your-app-name your-dockerhub-username/your-app-name
docker tag customer-churn-prediction matteopilotto/customer-churn-prediction

docker push your-dockerhub-username/your-app-name
docker push matteopilotto/customer-churn-prediction

docker pull your-dockerhub-username/your-app-name
docker pull matteopilotto/customer-churn-prediction

docker run -d -p 8501:8501 your-dockerhub-username/your-app-name
docker run -d -p 8501:8501 matteopilotto/customer-churn-prediction

docker buildx build --platform linux/amd64,linux/arm64 -t your-dockerhub-username/your-app-name --push .
docker buildx build --platform linux/amd64,linux/arm64 -t matteopilotto/customer-churn-prediction --push .

docker pull --platform linux/amd64 your-dockerhub-username/your-app-name
docker pull --platform linux/amd64 matteopilotto/customer-churn-prediction

